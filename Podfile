# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

platform :ios, '10.0'
use_frameworks!
inhibit_all_warnings!

def common_pods
    pod 'SwiftGen', '= 5.3.0'
    pod 'Reusable', '= 4.0.4'
end


target 'MonoTest' do
    common_pods
end
